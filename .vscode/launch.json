{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Roach Script",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/roach.go",
            "args": ["${file}"],
            "cwd": "${fileDirname}"
        }
    ]
}