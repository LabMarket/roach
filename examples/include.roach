# function useless
function today(day) { 
    let msg = "Every day is " + day + "\n"
    return msg; 
}

/*
    Just say that the useless function was created
*/
m = "function today was included!!!\n"
puts(m);
